# Changelog

## Unreleased

### Added
- New root `reachy` CLI for agent-friendly workflows.
- Read-only inspection commands for discovery, doctor, daemon status, state, camera specs, and artifact capture.
- Motion preview command for non-live planning.
- Explicit `--live` safety gates for daemon restart and app publishing.
- Installed-command verification script at `scripts/verify_agent_cli.py`.

### Changed
- Updated README, AGENTS guidance, and skills to prefer the new root CLI for agent tasks.
